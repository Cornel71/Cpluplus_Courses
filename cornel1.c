#include<stdio.h>
#include<string.h>

typedef struct {
    char nume[50];
    int varsta;
    int vechime;
} ANGAJAT;


int main()
{
    ANGAJAT angajat[5];
    int n; // nr de angajati
    int i;

    printf("cati angajati vreti sa introduceti?");
    scanf("%d", &n);

    citireAngajati (angajat, 5);

    // afisam angajatii
    printf ("\nAfisarea tuturor angajatilor:\n");
    for (i=0; i<n; i++)

    for (i=0; i<n; i++)
    {
        if
        (angajat[i].varsta > 40 == 0)
        {
            contor++;
            poz[j] = i;
            j++;
        }

    }

    {
        printf ("NU avem un astfel de angajat");
    }
       return 0;
}
