// afisarea unei valori de tip float

#include <stdio.h>
#include <limits.h>

int main()
{
    float x = 7.675, y = 6, z = 9;
    int j = -123;

    printf("Numarul x are valoarea %.2f, numarul y are valoarea %f", x, y);

    printf("\nNumarul x are valoarea %f, iar j are valoarea %d", x, j);

    return 0;
}

